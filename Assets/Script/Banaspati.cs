using System.Collections;
using System.Collections.Generic;
// using System.Numerics;
using UnityEngine;

public class Banaspati : MonoBehaviour
{
    // [SerializeField]
    // Transform rayPoint;

    // public float rayDistance;

    // Vector2 dir;

    // [SerializeField]
    // Transform player;
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

        // if(Vector2.Distance(player.position,rayPoint.position) <= 10)
        // {


        // }
        // RaycastHit2D hitInfo = Physics2D.Raycast(rayPoint.position,Vector2.right,rayDistance);
        // Debug.DrawRay(rayPoint.position,Vector2.left * rayDistance, Color.green);
        
    }


}
